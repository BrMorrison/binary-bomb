This phase involves arbitrary code execution and really messing with the stack. The idea will be to have the player fill the buffer with code and use the return address override to have the return instruction jump to the code written into the buffer.
